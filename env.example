# =============================================================================
# RabbitMQ Configuration
# =============================================================================
# RabbitMQ connection URL with authentication
# Format: amqp://username:password@host:port/vhost
# Default credentials: guest/guest (change in production!)
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Main queue name for processing messages
RABBITMQ_QUEUE=orders_queue

# Dead Letter Queue - messages that fail after max retries go here
RABBITMQ_DLQ=orders_dlq

# =============================================================================
# Salesforce Configuration
# =============================================================================
# Salesforce instance URL (base URL, not Lightning setup page)
# For developer/sandbox orgs: https://yourinstance--dev-ed.develop.my.salesforce.com
# For production: https://yourinstance.salesforce.com
# Remove any /lightning/... paths from the URL
SALESFORCE_INSTANCE_URL=https://yourinstance.salesforce.com

# Connected App Consumer Key (Client ID)
# Found in: Setup → App Manager → Your Connected App → Consumer Key
SALESFORCE_CLIENT_ID=your_client_id

# Connected App Consumer Secret (Client Secret)
# Found in: Setup → App Manager → Your Connected App → Consumer Secret
# Click "Click to reveal" to see the secret
SALESFORCE_CLIENT_SECRET=your_client_secret

# Salesforce username (EXACT value from Setup → Users → All Users → Username column)
# NOT your Full Name or Contact Email - use the exact Username column value
SALESFORCE_USERNAME=your_username@example.com

# Salesforce password (MUST include security token combined)
# Format: <your_password><security_token> (NO space between them!)
# Example: MyPassword123ABC123TOKEN
# IMPORTANT: Combine password + security token here, no space!
SALESFORCE_PASSWORD=your_password_your_security_token

# Salesforce Security Token (kept for reference, but already included in PASSWORD above)
# Get it from: Setup → My Personal Information → Reset My Security Token
# Check your email for the token after resetting
# NOTE: This token is already combined with password in SALESFORCE_PASSWORD above
SALESFORCE_SECURITY_TOKEN=your_security_token

# =============================================================================
# API Configuration
# =============================================================================
# Port for the Producer API server
API_PORT=3000

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: error, warn, info, debug
LOG_LEVEL=info

# Directory where log files are stored
LOG_DIR=./logs
